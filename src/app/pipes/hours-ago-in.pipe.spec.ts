import { HoursAgoInPipe } from './hours-ago-in.pipe';

describe('HoursAgoInPipe', () => {
  it('create an instance', () => {
    const pipe = new HoursAgoInPipe();
    expect(pipe).toBeTruthy();
  });
});
